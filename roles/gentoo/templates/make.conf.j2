CFLAGS="-O2 -pipe"
CXXFLAGS="${CFLAGS}"
CHOST="x86_64-pc-linux-gnu"
USE="mmx sse sse2"

PORTDIR="/usr/portage"
DISTDIR="${PORTDIR}/distfiles"
PKGDIR="${PORTDIR}/packages"

MAKEOPTS="-j {{ ansible_processor_vcpus | default('8') }}"

VIDEO_CARDS="{{ gentoo_video_cards }}"
INPUT_DEVICES="{{ gentoo_input_devices }}"
